"""chucknorris_ext — sidecar modules for Chuck Norris (skills, tool specs).

Kept as a separate package (Basilisk-style) so the base app stays lean and new
capability can be added here without bloating the per-turn system prompt.
"""
